
export default function CrudTable({title,rows=[],fields=[],onAdd,onDelete}){return <section className="card"><div className="section-head"><h2>{title}</h2>{onAdd&&<button onClick={onAdd}>+ Nuevo</button>}</div><div className="table-wrap"><table><thead><tr>{fields.map(f=><th key={f.key}>{f.label}</th>)}<th>Acción</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}>{fields.map(f=><td key={f.key}>{Array.isArray(r[f.key])?r[f.key].join(', '):String(r[f.key]??'')}</td>)}<td>{onDelete&&<button className="ghost danger" onClick={()=>onDelete(r.id)}>Eliminar</button>}</td></tr>)}</tbody></table></div></section>}
