import { createProductNotImplemented } from '../services/notImplemented';

export const productConfigurationAdapter = Object.freeze({
  toProductConfiguration: createProductNotImplemented('productConfigurationAdapter.toProductConfiguration'),
});
