import { createProductNotImplemented } from '../services/notImplemented';

export const productTemplateAdapter = Object.freeze({
  toProductTemplate: createProductNotImplemented('productTemplateAdapter.toProductTemplate'),
});
