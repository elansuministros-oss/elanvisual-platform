import { createProductNotImplemented } from '../services/notImplemented';

export const productDefinitionEngine = Object.freeze({
  validateDefinition: createProductNotImplemented('productDefinitionEngine.validateDefinition'),
});
