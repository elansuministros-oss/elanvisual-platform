import { createProductNotImplemented } from '../services/notImplemented';

export const templateResolver = Object.freeze({
  resolveTemplate: createProductNotImplemented('templateResolver.resolveTemplate'),
});
