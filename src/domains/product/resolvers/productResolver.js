import { createProductNotImplemented } from '../services/notImplemented';

export const productResolver = Object.freeze({
  resolveProduct: createProductNotImplemented('productResolver.resolveProduct'),
});
