import { createProductNotImplemented } from '../services/notImplemented';

export const configurationResolver = Object.freeze({
  resolveConfiguration: createProductNotImplemented('configurationResolver.resolveConfiguration'),
});
