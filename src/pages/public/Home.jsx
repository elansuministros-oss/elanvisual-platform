
import{Link}from'react-router-dom';import{useElan}from'../../core/context/ElanContext.jsx';
export default function Home(){const{banners,categorias,productos}=useElan();const b=banners.find(x=>x.estado==='Activo')||banners[0];return <main><section className="hero" style={{backgroundImage:`linear-gradient(90deg,rgba(0,0,0,.72),rgba(0,0,0,.20)),url(${b?.imagen})`}}><div><span>ELANVISUAL</span><h1>{b?.titulo}</h1><p>{b?.subtitulo}</p><Link className="primary" to={b?.enlace||'/catalogo'}>{b?.boton||'Ver Catálogo'}</Link></div></section><section className="container"><h2>Categorías principales</h2><div className="grid cats">{categorias.slice(0,6).map(c=><Link className="card" to={`/catalogo/${encodeURIComponent(c.nombre)}`} key={c.id}><h3>{c.nombre}</h3><p>{c.subcategorias.slice(0,4).join(' · ')}</p></Link>)}</div><h2>Trabajos y productos destacados</h2><div className="grid products">{productos.slice(0,6).map(p=><article className="product" key={p.id}><img src={p.imagen}/><h3>{p.nombre}</h3><p>{p.categoria}</p></article>)}</div><div className="trust">Producción real, materiales verificables, cotización clara y seguimiento por Orden de Trabajo.</div></section></main>}
