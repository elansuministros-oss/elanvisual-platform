
import{Link,useParams}from'react-router-dom';import{useElan}from'../../core/context/ElanContext.jsx';
export default function Catalogo(){const{categoria}=useParams();const{categorias,productos,agregarCarrito,crearLead}=useElan();const lista=categoria?productos.filter(p=>p.categoria===decodeURIComponent(categoria)):productos;return <main className="container"><h1>Catálogo</h1><div className="chips">{categorias.map(c=><Link key={c.id} to={`/catalogo/${encodeURIComponent(c.nombre)}`}>{c.nombre}</Link>)}</div><div className="grid products">{lista.map(p=><article className="product" key={p.id}><img src={p.imagen}/><span>{p.categoria} / {p.subcategoria}</span><h3>{p.nombre}</h3><p>{p.tipo==='especial'?'Presupuesto especial':`Desde USD ${p.precioVenta}`}</p>{p.tipo==='estandar'?<button onClick={()=>agregarCarrito(p,1)}>Agregar al Carrito</button>:<button onClick={()=>{crearLead({producto:p.nombre,cliente:'Pendiente',observacion:'Solicitud desde catálogo'});alert('Solicitud creada como lead.')}}>Solicitar Cotización</button>}</article>)}</div></main>}
